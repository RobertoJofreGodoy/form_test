<%= form_with(model: assessment, local: true) do |form| %>
  <% if assessment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assessment.errors.count, "error") %> prohibited this assessment from being saved:</h2>

      <ul>
      <% assessment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :inhabited %>
    <%= form.check_box :inhabited %>
  </div>


  <h2>Propiedades</h2>
  <%= form.fields_for :properties do |property|%>
    <div class="properties_fields"> 
      <div class="fields">  
          <%= property.label :number, "numero Propiedad:"%>
          <%= property.number_field :number%><br>
          <%= property.label :name, "nombre:"%>
          <%= property.text_field :name%><br>      
      </div>
    </div>
  <% end %> 

    <div class="actions">
    <%= form.submit %>
  </div> 
<% end %>
